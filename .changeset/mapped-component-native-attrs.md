---
"@zod-to-form/codegen": patch
---

Mapped/named field components now receive schema-derived native attributes (e.g. `type="email"`) like raw inputs do, so generated forms keep native input semantics when using a component map.
